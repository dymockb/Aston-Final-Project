{
    "select-all-shows": "select * from ShowDetail;",
    "browse-shows": "SELECT ShowDetail.ID, ShowName, ShowDescription, ShowType, Duration FROM ShowDetail JOIN TypeOfShow ON TypeOfShow.ID = ShowDetail.ShowTypeID;"
}

